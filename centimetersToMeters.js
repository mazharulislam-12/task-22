function centimetersToMeters(centimeters) {
    return centimeters / 100;
}
const centimeters = 250;
const meters = centimetersToMeters(centimeters);
console.log(meters);

// 01. practice ----------
function centimetersToMeters(centimeters) {
    return centimeters / 100;
}
const centimeters = 200;
const meters = centimetersToMeters(centimeters);
console.log(meters);

// 02. practice ------------
function centimetersToMeters(centimeters) {
    return centimeters / 100;
}
const centimeters = 500;
const meters = centimetersToMeters(centimeters);
console.log(meters);